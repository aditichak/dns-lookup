
Program uses Java and UDP datagrams to implement a scaled back DNS resolver client. The client can interact with various DNS servers to resolve a domain name into an IP address (IPV4 addresses only)

